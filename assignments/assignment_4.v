(*
  Домашнее задание 4 (Coq: типы, функции, доказательства)
  
  Дедлайн: 25 декабря 2025, 23:59
  Задачи со звёздочкой принимаются до конца семестра.
  
  Сдача: файл .v на почту m.voronov@gse.cs.msu.ru
  Вопросы: по почте или в ТГ-группе курса.
*)

Module Assignment4.

    (* Заглушка для незаполненных определений — замените на реальный код *)
    Axiom todo : forall {A : Type}, A.

    (*
    ============================================================
    1. Реализуйте следующие логические операции на bool:
    ============================================================
    *)
    
    (*
        2 балла,
        логическое "или"
    *)
    Definition orb (a b : bool) : bool := 
      false.
    
    (*
        2 балла,
        исключающее "или" (xor)
    *)
    Definition xorb (a b : bool) : bool := 
      false.
    
    (*
        2 балла,
        равенство bool аргументов (реализуйте без использования eq)
    *)
    Definition eqb (a b : bool) : bool :=
      false.
    
    (*
    ============================================================
    2. Реализуйте следующие операции над натуральными числами:
    ============================================================
    *)
    
    Inductive nat : Type :=
    | O : nat
    | S : nat -> nat.

    Fixpoint plus (n m : nat) : nat :=
      match n with
      | O => m
      | S n' => S (plus n' m)
      end.
        
    (*
        2 балла,
        уменьшение числа на 2:
        sub2 3 = 1
        sub2 1 = 0
        sub2 0 = 0
    *)
    Definition sub2 (n : nat) : nat :=
      O. (* заглушка *)
    
    (*
        3 балла,
        вычитание чисел (усечённое):
        sub 5 3 = 2
        sub 3 5 = 0
    *)
    Fixpoint sub (m n : nat) : nat :=
        todo.
    
    (*
        3 балла,
        деление числа на 2 (целочисленное):
        div2 5 = 2
        div2 4 = 2
        div2 1 = 0
    *)
    Fixpoint div2 (n : nat) : nat :=
        todo. 
    
    (*
        2 балла,
        сравнение чисел на равенство
    *)
    Fixpoint eqn (m n : nat) : bool :=
        todo.
    
    (*
        2 балла,
        сравнение чисел: меньше или равно
    *)
    Fixpoint leq (m n : nat) : bool :=
        todo.

    (*
    ============================================================
    3. Реализуйте следующие законы пропозициональной логики
       (используя соответствие Карри-Ховарда)
    ============================================================
    *)
    
    Variables A B C : Prop.
    
    (* 2 балла: из конъюнкции следует первый конъюнкт *)
    Definition and_fst :
      A /\ B -> A
    := 
        todo.
    
    (* 2 балла: транзитивность импликации *)
    Definition impl_trans :
      (A -> B) -> (B -> C) -> A -> C
    :=
        todo.
    
    (* 3 балла: аксиома S комбинаторной логики (Hilbert S) *)
    Definition HilbertS :
      (A -> B -> C) -> (A -> B) -> A -> C
    :=
        todo.
    
    (* 3 балла: тройное отрицание влечёт одинарное *)
    Definition DNE_triple_neg :
      ~ ~ ~ A -> ~ A
    :=
        todo.
    
    (* 2 балла: коммутативность дизъюнкции *)
    Definition or_comm :
      A \/ B -> B \/ A
    :=
        todo.

    (*
    ============================================================
    4. Доказательства в тактическом стиле
    ============================================================
    *)
    
    (* 3 балла: из лжи следует что угодно (ex falso quodlibet) *)
    Theorem ex_falso (F: False): S O = S (S O).
    Proof.
    Admitted.
    
    (* 6 баллов: коммутативность сложения *)
    Theorem plus_comm: forall m n, plus m n = plus n m.
    Proof.
      (* Подсказка: понадобится индукция и вспомогательные леммы *)
    Admitted.
    
    (*
    ============================================================
    5. Деревья
    ============================================================
    *)
    
    Inductive tree : Type :=
    | leaf : tree
    | node : tree -> tree -> tree.
    
    (* 3 балла: функция подсчёта высоты дерева *)
    (* height leaf = 0 *)
    (* height (node l r) = 1 + max(height l, height r) *)
    Fixpoint height (t : tree) : nat :=
        todo.
    
    (* Для height вам может понадобиться функция max *)
    Fixpoint max (n m : nat) : nat :=
      match n, m with
      | O, _ => m
      | _, O => n
      | S n', S m' => S (max n' m')
      end.

    (*
    ============================================================
    6. Чётность и нечётность: взаимная рекурсия
    ============================================================
    
    Это пример взаимной рекурсии (mutual recursion):
    две функции определяются одновременно, вызывая друг друга.
    
    even 0 = true      odd 0 = false
    even 1 = false     odd 1 = true  
    even 2 = true      odd 2 = false
    ...
    
    Ключевая идея: n чётно == (n-1) нечётно
    *)
    
    Fixpoint even (n : nat) : bool :=
      match n with
      | O => true
      | S m => odd m
      end
    with odd (n : nat) : bool :=
      match n with
      | O => false
      | S m => even m
      end.
    
    (* 
       Вспомогательная функция: 
    *)
    Definition negb (b : bool) : bool :=
      match b with
      | true => false
      | false => true
      end.
    
    (* 
       3 балла: докажите, что even и odd — отрицания друг друга.
       Подсказка: нужна индукция, и в индуктивном шаге — 
       использовать гипотезу для обоих случаев (even и odd).
    *)
    Theorem even_odd_negb : forall n : nat, even n = negb (odd n).
    Proof.
    Admitted.
    
    (* 
       3 балла: докажите, что сумма двух чётных чисел чётна.
       Подсказка: индукция по первому аргументу, 
       понадобится вспомогательная лемма про odd.
    *)
    Theorem even_plus_even : forall m n : nat,
      even m = true -> even n = true -> even (plus m n) = true.
    Proof.
    Admitted.

    (*
    ============================================================
    7. Объясните простыми словами для семилетнего ребёнка:
    ============================================================
    
    a) (3 балла) Что такое "доказательство" в Coq и почему вообще программа 
       может проверить, корректно ли доказательство математической теоремы? 
       Приведите аналогию из повседневной жизни.
    
    b) (3 балла) Что означает соответствие Карри-Ховарда на примере:
       почему функция типа A -> B является "доказательством" того, 
       что из A следует B? Используйте понятную метафору 
    
    Напишите ваши объяснения в комментарии ниже:
    *)
    
    (*
    ОТВЕТ НА ЗАДАНИЕ 7:
    
    a) Что такое доказательство в Coq:
    
    
    b) Соответствие Карри-Ховарда:
    
    
    *)

    (*
    ============================================================
    8. (5 баллов)* Докажите следующее утверждение
    ============================================================
    
    Подсказка: рассмотрите случаи для b и c
    *)
    
    Definition andb (a b : bool) : bool :=
      match a with
      | true => b
      | false => false
      end.
    
    Theorem andb_eq_orb :
      forall (b c : bool),
      (andb b c = orb b c) -> b = c.
    Proof.
    Admitted.
    
    (*
    ============================================================
    9. Peirce's Law и классическая логика
    ============================================================
    
    Закон Пирса: ((A -> B) -> A) -> A
    
    a) (3 балла)* Покажите, что закон Пирса влечёт LEM
    b) (5 баллов)* Покажите, что LEM влечёт закон Пирса
    
    Это демонстрирует, что закон Пирса эквивалентен 
    классической логике и не выводим в интуиционистской.
    *)
    
    Definition LEM := 
        forall P : Prop, P \/ ~ P.
    
    Definition Peirce := forall A B : Prop, ((A -> B) -> A) -> A.
    
    Theorem Peirce_implies_LEM : Peirce -> LEM.
    Proof.
    Admitted.
    
    Theorem LEM_implies_Peirce : LEM -> Peirce.
    Proof.
    Admitted.

    (*
    ============================================================
    10. (6 баллов)* Деление натуральных чисел
    ============================================================
    
    Реализуйте целочисленное деление:
      div 7 3 = 2
      div 3 5 = 0
      div 5 0 = 0 (или любое значение - деление на 0 не определено)
    
    Подсказка: можно использовать вспомогательную функцию с "топливом" (fuel)
    или рекурсию по первому аргументу с вычитанием.
    *)
    
    Fixpoint div (m n : nat) : nat :=
      O. (* заглушка - реализуйте сами *)

    (*
    ============================================================
    11. (4 балла)* Ассоциативность сложения
    ============================================================
    *)
    
    Theorem plus_assoc: forall m n k, plus (plus m n) k = plus m (plus n k).
    Proof.
    Admitted.

    (*
    ============================================================
    12. (6 баллов)* Свойства деревьев
    ============================================================
    
    Докажите, что количество листьев в дереве равно 
    количеству внутренних узлов + 1.
    *)
    
    Fixpoint count_leaves (t : tree) : nat :=
      match t with
      | leaf => S O
      | node l r => plus (count_leaves l) (count_leaves r)
      end.
    
    Fixpoint count_nodes (t : tree) : nat :=
      match t with
      | leaf => O
      | node l r => S (plus (count_nodes l) (count_nodes r))
      end.
    
    Theorem leaves_nodes : forall t : tree,
      count_leaves t = S (count_nodes t).
    Proof.
    Admitted.

    (*
    ============================================================
    13. (4 балла)* Рефлексия: связь bool и Prop
    ============================================================
    
    Докажите, что eqn корректно отражает равенство:
    если eqn m n = true, то m = n.
    
    Это пример "рефлексии" - связи между вычислительным 
    и пропозициональным миром (важная тема для Coq/SSReflect).
    *)
    
    Theorem eqn_true_iff : forall m n : nat,
      eqn m n = true -> m = n.
    Proof.
    Admitted.

    (*
    ============================================================
    14. (10 баллов)* Эквивалентность формулировок LEM
    ============================================================
    
    Докажите эквивалентность двух формулировок закона 
    исключённого третьего.
    *)
    
    Definition LEM_forall := 
        forall (A: Type) (P: A -> Prop) (Q: Prop),
            (forall x: A, Q \/ P x) <-> (Q \/ forall x: A, P x).
    
    Theorem LEM_equiv: LEM <-> LEM_forall.
    Proof.
    Admitted.

End Assignment4.
